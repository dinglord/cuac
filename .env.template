# Copy to .env and replace values as needed
# External docker network where your Caddy container lives:
CADDY_NETWORK=caddy

# Hostname you’ll serve Guacamole on via Caddy
GUAC_FQDN=guac.example.com

# Image tags
GUAC_VERSION=1.6.0
POSTGRES_VERSION=16

# Paths (keep defaults unless you move folders)
PROJECT_DIR=./
CONFIG_DIR=./config
RECORDINGS_DIR=./recordings
DB_DIR=./db

# Database name (safe to commit)
POSTGRES_DB=guacamole_db